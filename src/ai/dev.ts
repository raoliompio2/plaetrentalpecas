import { config } from 'dotenv';
config();

// No flows are currently defined for the application.
// The manufacturer description generator has been removed due to a flawed workflow.
// AI features should be introduced in a context that provides value to the user,
// such as a tool within the manufacturer's own profile page to help them refine their content.
